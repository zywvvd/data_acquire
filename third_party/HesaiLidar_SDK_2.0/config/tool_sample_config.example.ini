# 复制为 tool_sample_config.ini 后修改路径。
# ./pcl_tool /path/to/tool_sample_config.ini
# ./las_tool /path/to/tool_sample_config.ini
# 末尾加 1 可强制 use_gpu=true
#
# [source_type] 选择数据源场景，具体参数写在对应节 [network]、[pcap]、[serial]、[ros]、[tcp]。
# 只有与 source_type 匹配的节会被使用，其他节的参数会被忽略。
#
# source_type 取值：
#   network | lidar | online  -> 使用 [network] 节（在线 UDP）
#   pcap | offline            -> 使用 [pcap] 节
#   serial                    -> 使用 [serial] 节
#   ros | external            -> 使用 [ros] 节（外部输入）
#   tcp | lidar_tcp           -> 使用 [tcp] 节（在线 TCP）

[source_type]
source_type = pcap

# ==================== 数据源配置（根据 source_type 选择使用） ====================

[pcap]
pcap_path = Your pcap file path
correction_file_path = Your correction file path
firetimes_path = Your firetime file path

[network]
device_ip_address = 192.168.1.201
ptc_port = 9347
udp_port = 2368
multicast_ip_address =
use_ptc_connected = true
correction_file_path = Your correction file path
firetimes_path = Your firetime file path
host_ip_address =
fault_message_port = 0
ptc_mode = tcp
certFile =
privateKeyFile =
caFile =

[tcp]
device_ip_address = 192.168.1.201
device_tcp_src_port = 5121
ptc_port = 9347
use_ptc_connected = true
correction_file_path = Your correction file path
firetimes_path = Your firetime file path

[serial]
rs485_com = Your serial port name for receiving point cloud
rs232_com = Your serial port name for sending cmd
point_cloud_baudrate = 3125000
correction_save_path =
correction_file_path = Your correction file path

[ros]
correction_file_path = Your correction file path
firetimes_path = Your firetime file path

# ==================== 通用配置 ====================

[driver]
use_gpu = false

[decoder]
pcap_play_synchronization = true
play_rate = 1.0
pcap_play_in_loop = false
socket_buffer_size = 262144000

# ==================== PCL 工具选项 ====================
# 使用pcl_tool.cc时，需要配置此节，否则配置无效
[pcl]
save_pcd_ascii = true
save_pcd_binary = false
save_pcd_binary_compressed = false
save_ply = false
enable_viewer = false
pcd_ascii_precision = 16
output_dir = out_pcd
output_dir_with_timestamp = false
save_valid_points_only = false

# ==================== LAS 工具选项 ====================
# 使用las_tool.cc时，需要配置此节，否则配置无效
[las]
save_las = false
save_laz = true
output_dir = out_las
output_dir_with_timestamp = false

